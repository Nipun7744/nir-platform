{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm ci && npm run build:shared && npx prisma generate --schema apps/api/prisma/schema.prisma && npm run build:api"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema apps/api/prisma/schema.prisma && node apps/api/dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
